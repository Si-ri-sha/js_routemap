//function in js to sort a array consiisting of strings

function sortAlphabetically(arr) {
  return arr.sort(); //by default sort is built-in to sort alphabetically
}

console.log(sortAlphabetically(["B", "N", "C" , "S", "L" ]));
